# T039 任務完成摘要 — 模型備援鏈 YAML 配置＋順位重排（2026-08-09）

## 背景
`twin auto` 原本硬編備援順位（OpenRouter → ollama → opencode CLI → relay），
且各 tier model 不可配置。應使用者要求改為 ① opencode CLI headless 第一（deepseek-v4-flash-free）② OpenRouter ③ ollama ④ relay，並支援 YAML 配置。

## 完成內容
1. **`.opencode/impl_providers.yaml`**（新增）：4 個 tier × `enabled`/`model`，順序即優先序；
   檔案不存在時退回內建順位
2. **`auto_develop.py`**：
   - `load_impl_providers()`：yaml 讀取＋快取＋解析失敗 fallback
   - `call_model_for_implementation` 重寫為依配置順位迭代各 tier（含 acceptable 驗證、重試 2 次、last_exc 累積）
   - `_tier_model()`：tier model 空值→內建預設；`--model` CLI 只覆蓋 openrouter tier（向後相容）
   - `_do_call_opencode(prompt, model=None)`（預設 deepseek-v4-flash-free）、`_do_call_local(prompt, model=None)`（預設 auto-fastest）
3. **文件**：README §6、docs/development-workflow.md 情境 B 補配置說明
4. **測試**：`tests/test_impl_providers.py` 4 項離線測試

## 驗證
- 4 測試：內建順位 fallback／YAML 順位＋disabled／opencode 第一優先（--model 不影響）／失敗轉 openrouter＋覆蓋
- 全量 128 passed + 1 skipped（+4）；ruff/format 全過
- 程式碼 commit `abcbdc8`（含變更摘要）

## 影響
- 既有 `--model` 語意保留（僅跑 OpenRouter tier）
- 無 YAML 時行為＝內建順位（平滑升級）