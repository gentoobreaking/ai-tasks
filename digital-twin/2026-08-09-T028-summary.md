# T028 任務完成摘要 — DiscussionOrchestrator 回歸測試（2026-08-09）

## 目的
T017 P0 修復（`system_prompt`/`template_path` 未在 `__init__` 初始化 → 任何 `twin discuss` 必崩）的**回歸防護**。

## 完成內容
1. **`discussion_orchestrator/orchestrator.py`**：
   - 重建 `_build_messages(round_num, model_name, previous_outputs=None) -> list[dict]`
     （回歸 API：`[{role:system, content:system_prompt}, {role:user, content:輪次指示}]`）
   - `_build_prompt` 新增 `previous_outputs` 參數（顯式傳入時優先於 `self.all_outputs`，
     `run()` 呼叫不變，行為零影響）
2. **`tests/test_discussion_orchestrator.py`** 新增 5 項測試（全離線）：
   - `__init__` 後 `system_prompt` 非空（P0 復發防護）
   - 模板不存在 → 內建預設提示詞（含「資深工程師」與版本號）
   - 模板存在 → 優先讀取模板內容（marker 驗證）
   - `_build_messages(round=0)` → messages[0]=system（== system_prompt）、messages[1]=user（含角色與第 1 輪）
   - `_build_messages(round>0)` → user 含「聚焦增量價值」、上一輪輸出、最後一輪「最終收斂」

## 驗收結果（全數通過）
- 離線：建立前 adapters={}，0 網路請求
- 全量 pytest：124 passed + 1 skipped（原 119 + 新增 5）
- ruff check tests/ discussion_orchestrator/ → All checks passed
  （順手修正 T033 時代的 UP037 引號註解、test_spec_auto_merge I001 import 排序）
- 手動 `./twin discuss --help` → exit 0、無 AttributeError（CLI 無 dry-run，以單元測試為準）

## 備註
- 2026-08-05 任務書撰寫時 `_build_messages` 尚存在；T021 重構已改名 `_build_prompt`（單一 prompt 字串）。
  本次依驗收標準重建 `_build_messages` 為訊息組裝 API，並保留 `run()` 既有路徑不變。
- README §2 新增回歸防護一行說明。

## 影響
- `orchestrator.py` 僅新增/擴充方法，無行為改變；ruff 修正為既有檔案清潔。