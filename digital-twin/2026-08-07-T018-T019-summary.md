# T018/T019 任務完成摘要 — depends_on 依賴 + PR 摘要閘門

- **狀態**: done
- **Commit**: 3f9f126
- **日期**: 2026-08-07

## T018 交付內容

1. **任務模板** `~/Projects/ai-skills/clw-ideas2tasks/templates/task-template.md` 新增 `depends_on: []`
2. **auto_develop.py 依賴機制**
   - `Task.depends_on` 解析（支援 `T012` / `12` / 清單 / 逗號字串）
   - `task_dependencies()` / `find_blockers()` 依賴檢查
   - `get_next_pending_task()` 與 `run()` 主循環:依賴未 done 時跳過並印原因
   - `--list` 顯示依賴資訊(如 `T014 (depends: T012)`)
3. **路徑收斂**(依使用者規格修正):任務檔與開發文件統一於 `~/tasks/<project>/`;`PROJECT_PATHS.tasks_dir` 指向該處;**不建立** `~/Projects/<project>/tasks/`;記錄於 README「路徑規範（T018）」章節
4. **35 個既有任務檔**補上 `depends_on` 欄位:T006/T008→T003、T014→T012,其餘空陣列

## T019 交付內容

1. **`build_pr_summary()` / `save_pr_summary()`**:commit 前產出 `logs/pr-T0XX.md`(檔案清單表格 + 每檔 +/- 行數 + 說明 + 總計)
2. **`count_diff_lines()`**:以 `git diff --cached --numstat HEAD` 計算,先 stage 確保未追蹤新檔計入;`_should_exclude()` 排除 logs/、.env、.venv/
3. **`check_commit_gate()`**:diff ≥ `--confirm-threshold`(預設 500)時:
   - 互動模式:詢問 y/N
   - 非互動(`--once`/背景/CI):跳過 commit 並提示檢視 PR 摘要
4. **`--no-confirm`** 參數關閉閘門
5. 小 diff 行為不變(不影響 T012 正常路徑)

## 驗收結果

| T018 驗收項目 | 結果 |
|---|---|
| 模板 frontmatter 新增 depends_on: [] | ✅ |
| auto_develop 依賴未 done 跳過(印原因) | ✅ 測試覆蓋 |
| --list 顯示依賴資訊 | ✅ 3 個任務顯示 depends |
| 路徑收斂至 ~/tasks/<project>/ | ✅ README 記錄 |
| 既有任務檔補 depends_on | ✅ 35 個全補 |

| T019 驗收項目 | 結果 |
|---|---|
| commit 前產 PR 摘要 logs/pr-T0XX.md | ✅ |
| diff ≥ 閘門:互動詢問 / 非互動跳過 commit | ✅ 測試覆蓋 |
| 摘要含檔案清單 + 每檔 +/- 行數 + 說明 | ✅ |
| --no-confirm 關閉閘門 | ✅ |
| 不影響 T012 正常路徑 | ✅ 測試覆蓋 |

## 測試

- `tests/test_auto_develop_deps.py`:10 tests 全通過(depends_on 解析/依賴閘門/PR 摘要/閘門邏輯)
- 全套件:70 passed + 1 skipped
- auto_develop.py ruff debt 全清(35→0 errors,順帶清理 E501/E741/F841 等)
