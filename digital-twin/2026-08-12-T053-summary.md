# T053 任務完成摘要 — 移除 config/validate.py 死碼（2026-08-12）

## 完成內容
1. **刪除 config/validate.py（118 行死碼）**：
   - `get_settings` / `validate_config` / `validate_config_silent` 全 repo 零引用
   - 其「openrouter/gemini key 必填」設計與離線可測現況矛盾；模型名（grok-2、claude_model）
     與 `.opencode/models.yaml`（grok-4.5）不同步
   - key 檢查由 `./twin doctor`（env 檢查）承接；模型清單由 models.yaml 承接
2. **無新依賴**：`pydantic-settings` 未直接列於 pyproject（僅為 chromadb 的傳遞依賴），
   pyproject 不需更動

## 驗收
- [x] rg `validate`/`validate_config`/`config.validate` 於專案碼/測試零引用
- [x] README 與 docs/ 無 config/validate 引用
- [x] 不引入新依賴（pydantic-settings 為 chromadb 傳遞依賴，保留於 uv.lock）
- [x] pytest 全量：182 passed + 1 skipped；ruff 全過

## 備註
- specs/ai-consultations/v3 與 full_sessions.md 為歷史決策記錄，保留原文不動
- 修 pre-commit hook：ruff 檢查改 `--diff-filter=ACMRTUB` 排除已刪除 .py 檔，
  解鎖「刪除 .py 檔案」的提交（hook 缺陷順手修復）
- commit：`c9cbf4e`（refactor）