# T046 任務完成摘要 — PROJECT_PATHS 動態化（2026-08-09）

## 完成內容
1. **config.py**：
   - 移除寫死的 4 專案 `PROJECT_PATHS`，改為 `discover_projects(tasks_root, projects_root)`：
     - 掃描 `~/tasks/*/tasks/T*.md`，任一任務檔不含 `status: done`（正則含引號變體）
       即為 active（對應 `grep -L 'status: done' ... | uniq` 語意）
     - `code_dir = ~/Projects/{name}` 需存在才納入（避免僅有任務檔的專案）
     - `tasks_root`/`projects_root` 可注入 → 離線測試
   - `_load_projects_ignore()`：合併 `~/.projects_ignore` ＋ repo 根 `.projects_ignore`
     （每行一個、`#` 註解、空白略過）
2. **`.projects_ignore`（repo 根）**：排除 gold-analysis-advanced、gold-analysis-core、
   gold-monitor-issue、jarvis、md-viewer-app、revenue-zero-cost、sinotrade-scraper、taolive-ios

## 驗證
- `./twin projects`：digital-twin／tw-quant-signal／tw-quant-selector 維持；
  **tw-quant-mcp 依語意退出**（31 個任務全 status: done）
- tests/test_discover_projects.py 7 項（active／全 done／引號 done／無 code 目錄／
  ignore 檔／雙 ignore 合併／repo ignore 不含 twin）；全量 144 passed + 1 skipped
- ruff check/format 全過；程式碼 commit `70345c2`

## 行為變更（注意）
- `twin projects` 現在反映「有未完成任務＋有程式碼目錄＋未 ignore」的專案；
  專案全部完成後會自動離開清單（如 tw-quant-mcp）
- 排除清單可隨時編輯 `.projects_ignore`（repo 或家目錄）