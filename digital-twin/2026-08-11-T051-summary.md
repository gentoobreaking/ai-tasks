# T051 任務完成摘要 — auto_develop 拆分模組（2026-08-11）

## 完成內容
1. **auto_develop.py 1796 → 190 行**（<400 達標），僅保留 CLI 入口（main/argparse）＋薄轉發
2. **新增三個職責模組**（只搬不移，行為等價重排）
   - `providers.py`（326 行）：`build_implementation_prompt`、`load_impl_providers`、
     `call_model_for_implementation`（opencode → openrouter → ollama → local relay
     failover 鏈）、`_do_call_{model,ollama,local,opencode}`
   - `diff.py`（452 行）：diff 解析/套用/修剪（`strip_markdown`、`_normalize_path`、
     `apply_diff`、`_extract_unified_diff`、`_trim_incomplete_diff`、`_parse_model_output`、
     `_repair_diff`、`_diff_is_valid`）＋ T019 PR 摘要/commit 閘門（`git_commit`、
     `build_pr_summary`、`save_pr_summary`、`count_diff_lines`、`_should_exclude`）
   - `scheduler.py`（919 行）：任務挑選（`load_tasks`/`get_next_pending_task`/
     `find_blockers`/…）、`run_tests`（T012 三層閘門）、`sync_readme`（T049）、
     T023 blocked 流程（`generate_blocked_review`/`retry_task`/`supersede_task`/…）、
     `AutoDevelopScheduler` 主迴圈（T014 repair、T019 閘門、T013 還原）
3. **import 方向單向無循環**：scheduler 吃 providers/diff/common.tasks；
   providers 吃 diff；providers/diff 不 import scheduler

## 驗證結果
- pytest 全量：164 passed + 1 skipped（與 T050 基線相同）
- ruff check 全過（ruff format 已套用）；pyright 12 errors（基線持平）
- CLI 冒煙：`auto_develop.py --list` / `--blocked` / `--sync-readme` 輸出與先前一致
- 關鍵測試原樣通過：test_auto_develop_deps（閘門/PR 摘要）、test_blocked_review、
  test_impl_defaults、test_impl_providers（備援鏈 failover）

## 備註
- 測試 monkeypatch 目標隨模組歸位（行為不變）：`PROJECT_PATHS` → scheduler/providers 模組、
  `_provider_cache`/`IMPL_PROVIDER_YAML`/`_do_call_*` → providers 模組、`load_tasks` →
  scheduler 模組；auto_develop 保留相容 re-export（tests 仍 `import auto_develop as ad`）
- T052（observability 接入）可直接以拆分後模組為基礎進行
- commit：`cc40d81`（refactor）