# T052 任務完成摘要 — auto_develop 接入 observability（2026-08-11）

## 完成內容
1. **scheduler.py 新增 6 個結構化事件**（structlog JSON stdout，與 CLI print 並存，
   print 一行未刪）：
   - `task_selected`：run() 挑選任務後（task_id/name/priority/status）
   - `task_started`：process_task 更新 in-progress 後（含 fail_count）
   - `task_succeeded`：完成後（含 commit_hash）
   - `commit_made`：commit 成功（含 commit_hash、total_changes）
   - `task_failed`：_record_failure（含 fail_count/max_retries/reason/blocked 旗標）
   - `diff_gate_rejected`：check_commit_gate 兩拒絕點（reason 區分
     non_interactive / user_refused，含 total_changes/threshold）
2. **失敗統計不變**：append_failure_log JSONL 格式原樣；print 訊息原樣；
   OTEL 降級沿 obse 現況（observability.get_logger 自帶處理，無 OTEL 時
   純 structlog JSON 不報錯）

## 驗證結果
- 新增 tests/test_observability_events.py 3 項（全程 monkeypatch 不觸網）：
  閘門拒絕事件欄位、task_failed fail_count、以及 run()→process_task 全流程
  事件鏈（task_selected→started→commit_made→succeeded ＋ 真實 git commit
  落盤）＋ 斷言無誤發 diff_gate_rejected
- pytest 全量：167 passed + 1 skipped（+3，基線未劣化）
- ruff check/format 全過；pyright 12 errors（基線持平）

## 備註
- monkeypatch 目標注意：scheduler 以模組內部名稱呼叫模型，測試須 patch
  `sch.call_model_for_implementation`（不是 providers 模組）
- T053+ 可參考本模式將 multi_ai_discuss 等核心流程逐步事件化
- commit：`4fcfa31`（refactor）