# T056 任務完成摘要 — Telegram 自動推播（完成/blocked/doctor 異常）（2026-08-12）

## 完成內容
1. **`common/notify.py` 共用推播函式（收斂 telegram 通道）**：
   - `send_telegram_notification(text, chat_ids=None)`：async 版本（httpx.AsyncClient）
   - `notify_telegram(text)`：同步版本（doctor 等 non-async 呼叫端）
   - `notify_background(text)`：async 流程 fire-and-forget；無 running loop 退回同步
   - URL 統一引自 config.TELEGRAM_SEND_URL（配合 T054）；收件人預設＝admin 白名單
     （TELEGRAM_ADMIN_IDS，RBAC 同源）＋ TELEGRAM_CHAT_ID
   - 無 token/chat_id → 靜默回傳 False；失敗僅 log、不拋例外
2. **三事件接入**：
   - auto 完成：scheduler.process_task 於 commit_made + done 後推「任務編號、
     commit hash、PR 摘要路徑（logs/pr-T…md）」
   - blocked：`_record_failure` 達 max_retries 標記 blocked 後推「任務編號、
     blocked review 路徑、失敗 reason」
   - doctor 異常：`_notify_anomalies` 依 `DOCTOR_TELEGRAM_NOTIFY`（預設 off；
     fail / warn / all 三階）推異常項目清單（含 T049 一致性檢查輸出）
3. **既有通道收斂**：worker.notify 改走共用 async 函式（指定 chat_id 給發起者；
   保留 validator 前置檢查）；orchestrator._alert_budget 改走 notify_background

## 驗收
- [x] auto 完成推「任務編號、PR summary 位置」（＋commit hash）
- [x] blocked 推「任務編號、blocked review 路徑、reason」
- [x] doctor FAIL 異常推播（DOCTOR_TELEGRAM_NOTIFY 開關，預設關閉；fail/warn/all 三階）
- [x] 共用既有 Telegram 通道、收斂共用函式（配合 T054 TELEGRAM_SEND_URL）
- [x] 無 token/chat_id 靜默跳過，不影響 auto/doctor 主流程
- [x] pytest 全量：191 passed + 1 skipped（+9 新推播測試，mock httpx 不觸網）；ruff 全過

## 備註
- 新測試 tests/test_notify.py 9 項：無 token／無收件人／admin 白名單去重／指定 chat_id／
  網路失敗靜默／同步包裝／background 無 loop 退回同步／URL 一致性／doctor 開關分階
- 推播為 best-effort fire-and-forget，scheduler 主流程零等待
- commit：`8a985c4`（feature）