# T059 任務完成摘要 — 測試涵蓋缺口補齊（2026-08-12）

## 完成內容
1. **`common/tasks.py`（TaskStore）專屬測試**（tests/test_tasks_store.py，12 項）：
   - parse_task_file 欄位／無 frontmatter 預設值／壞 YAML 降級／非 T*.md 回 None
   - TaskStore.list 排序（priority_order）、unknown project 錯誤、status_map、depends_on
   - TaskStore.find 跨專案（tasks_root 注入，不碰真實 ~/tasks）
   - update_fields 語意（YAML 欄位順序不重排、body 不含 frontmatter 原樣保留）
   - T037 降級防護（done+commit 拒絕回 pending/in-progress）＋ force 繞道
2. **task_advisor / auto_guardrail / install_hooks 離線測試**（tests/test_advisor_guardrail_hooks.py，14 項）：
   - task_advisor：monkeypatch search_knowledge_base，驗證 cloud/quant/docs agent 決策與風險分支
   - auto_guardrail：tmp_path 建臨時檔，驗證 key 掃描／.env 忽略／type-hint／Dockerfile USER
   - install_hooks：純字串內容（gitleaks/ruff/trufflehog/pytest）＋ git 目錄錯誤＋可執行權限＋uninstall
3. **multi_ai_discuss / index_knowledge / observability 離線測試**（tests/test_index_discuss_observability.py，11 項）：
   - AIClient 建構（fake adapter，不觸網）、call 訊息拆系統/使用者、classify_fatal_error、
     --list-models 路徑
   - index_knowledge：parse_doc_sections 章節切分／短段落略過、_search_impl 計分排序、
     search_knowledge_base 離線（monkeypatch NOTES_DIR/TASKS_DIR/CURRENT_DIR）、collect 隔離
   - observability 降級模式（_OTEL_AVAILABLE=False → meter None、builder None、record no-op）
4. **環境敏感測試全面隔離**：PV 全量掃描確認——所有 PROJECT_PATHS/Path.home()/~ 依賴皆以
   monkeypatch.setattr/setitem、tmp_path、明確 tasks_root 注入隔離；無任何測試讀真實 ~/tasks

## 驗收
- [x] common/tasks.py 專屬測試：parse_task_file 欄位/無 frontmatter/損壞 yaml、
  TaskStore.list 排序與 depends_on、find 跨專案、set_status/update_fields（含 T037 降級防護、
  force 繞道、body 不含 frontmatter 語意、YAML 欄位順序不重排）
- [x] task_advisor / auto_guardrail / install_hooks 各至少一個離線單元測試（不觸網、不裝 hooks）
- [x] multi_ai_discuss / index_knowledge 至少一個可離線執行的建構/解析測試（不呼叫模型）
- [x] 既有「依賴真實 ~/tasks」測試全面改 monkeypatch PROJECT_PATHS 或 TaskStore(projects=)
  （掃描確認無真實 ~/tasks 依賴；新測試皆注入隔離）
- [x] pytest 全量維持 151 passed + 1 skipped（新增後 233 passed + 1 skipped，+41）；ruff 全過

## 備註
- 測試全部採 adapters={}/mock 離線模式（fake adapter、monkeypatch 目錄）
- common/observability 僅驗證降級模式（無 OTEL時 structlog JSON/meter None 不報錯）
- TaskStore.find 之 DEFAULT_TASKS_ROOT 於 import 綁定，測試以 tasks_root 顯式注入避免
  依賴 Path.home monkeypatch 失效問題
- commit：`9b32fc8`（test）