# T067 Summary — Dockerfile 依賴補 tenacity

## 完成內容
- `Dockerfile` 依賴 pip install 清單補上 `tenacity>=8.2`（resilience.py:20 import）
- 同步 tighten `pybreaker>=1.0` → `>=1.4,<2`（T077 配合，避 2.x API drift）
- 驗證：選擇「補手工清單」而非 `pip install ".[prod]"` — 驗證過 hatchling 無 `[tool.hatch.build]` 設定導致 `.[prod]` 建構失敗；此專案未做 pip-installable wheel，改以 source-on-PATH 執行

## 驗證
- 在乾淨 venv（僅安裝 Dockerfile dep list）執行 healthcheck imports：
  `import config, auto_develop, multi_ai_discuss, consensus_eval, spec_auto_merge, common.observability` → 印 `healthy`
- 16 deps 全解析成功 (rc=0) 含 tenacity + pybreaker 1.4
- README 註記來源與 wheel 限制
- pytest 全量通過、ruff 全過
- (docker daemon 未啟動，未執行真正 docker build；以等效乾淨 venv + healthcheck import 驗證代替)

## 影響檔案
- Dockerfile（dep list：+tenacity, pybreaker bound）
- README.md（T067 部署註記）

## 備註
- 若未來將專案做 wheel（加 [tool.hatch.build.targets.wheel]），可改上 `pip install ".[prod]"` 去除手工清單 drift