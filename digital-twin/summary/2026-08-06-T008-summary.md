# T008 Summary: Agent Registry 動態路由

- 日期: 2026-08-06
- Commit: `e832edf`
- 狀態: done

## 核心：資料驅動的委派機制
| 舊（扁平星型） | 新（T008） |
|---|---|
| 4 Agent 硬編碼於各 script | `.opencode/agent_registry.yaml` 定義 agents + routing rules |
| 無法動態決定誰處理任務 | `Registry.route(task_tags) -> agent_id`，YAML 驅動 |

## 交付檔案
| 檔案 | 重點 |
|---|---|
| `.opencode/agent_registry.yaml` | 4 agents（my/cloud-arch/docs-sync/quant-dev）含 id/role/capabilities[]/model/version + 4 routing rules（quant→cloud→docs→default） |
| `agent_registry.py` | `Registry` 載入 YAML、`route(task_tags)` 回傳 agent_id、`extract_tags` 從任務 frontmatter/內文推導標籤、`find_task_file` 定位 `~/tasks/*/tasks/Txxx*.md`、CLI `route --task-id Txxx --auto` 回寫 `{task_file}.routing.json` |
| `twin` | 新增 `./twin route --task-id T002 --auto` 子命令（呼叫 agent_registry.py） |
| `.opencode/agents/my-clone.md` | SOP 4.1 步驟 3 新增：執行 `./twin route --task-id {T編號} --auto` 決定委派對象 |
| `tests/test_agent_registry.py` | 10 測試：YAML 載入、route 各類、rule 順序優先、extract_tags（frontmatter/fallback）、CLI 寫 routing.json |

## 驗證
- `pytest: 43 passed`（既有 33 + 新增 10）
- 端到端：
  - `twin route --task-id T002 --auto --project digital-twin` → docs-sync（前 matter `summary` 命中 rule-docs）
  - `twin route --task-id T004 --auto --project digital-twin` → cloud-arch（dockerfile/compose 命中 rule-cloud）
  - `twin route --tags quant,strategy` → quant-dev（明確標籤命中 rule-quant）
  - `twin route --tags git,secrets` → my（無命中，預設 my）
- routing.json 產出結構：task_id, task_file, task_tags, delegated_to, matched_rule, agent, routed_at
- ruff 新檔案 0 error（既有 twin lint 為預存）

## 設計決策
1. **YAML 單一真實來源**：agents + routing_rules 皆在 `.opencode/agent_registry.yaml`，新增 Agent 只改 YAML
2. **rule 順序即優先級**：routing_rules 依序檢查，第一條 keyword 任一命中即委派；`keywords: []` 為預設兜底
3. **自動標籤推導**：優先 frontmatter `tags: [a,b]`；否則內文英文關鍵字（過濾 stopwords，取前 15）
3. **routing.json 附檔寫法**：寫入 `{task_file}.routing.json`（如 `T002-gitignore-and-hooks.routing.json`），不建立子目錄，便於查閱與 git diff
4. **CLI 統一入口**：`./twin route` 保持與既有 twin 指令風格一致

## 後續建議
- 依實際任務分佈微調 routing_rules keywords（如 T002 的 `summary` 觸發 docs-sync，可視需求縮小關鍵字）
- `my-clone.md` 其他分身（cloud-arch/docs-sync/quant-dev）同步引用 Registry
- 可考慮 `twin route --list-agents`、`twin route --show-rules` 擴充