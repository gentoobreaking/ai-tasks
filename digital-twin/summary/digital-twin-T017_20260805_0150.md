# digital-twin T017 完成

- 時間：2026-08-05
- 目標：pyproject 修正（pyright 路徑、structlog 依賴、tests 目錄）
- 結論：✅ 完成並驗證

## 變更
1. pyproject.toml：pyright include → ["*.py","tests/**/*.py"]；structlog 補三處依賴；ruff ignore RUF001/002/003
2. tests/test_task_parser.py：5 單元測試（frontmatter 解析含中文/emoji、無 frontmatter、非法檔名、缺 status）
3. 真實 bug 修復（pyright 掃描發現）：
   - P0: DiscussionOrchestrator template_path/system_prompt 未初始化 → twin discuss 必崩 → __init__ 補上
   - P1: _do_call_ollama model: str=None → str | None=None
   - P1: OpenRouter content None → 防禦式處理

## 驗證
- pyright 掃描檔案 0→5+；真實型別錯誤 9→0（剩 5 環境誤報）
- pytest 5 passed（不再 rc=5）；ruff 486→100；structlog ✅

## SOP
- commit fbc975c push digital-twin；摘要 d55c0f0 push free-ai-router；任務書 T017 done
- 下一步：T016 → T018 → 其餘
