# T022 任務完成摘要 — setup_daemon 路徑驗證 + extract_feedback DB 路徑可設定

- **狀態**: done
- **Commit**: a69439a
- **日期**: 2026-08-07

## setup_daemon 路徑驗證

- `install` 前驗證 `BOT_SCRIPT`(telegram_bot.py)存在;不存在 → 明確報錯(依賴 T006)且不產出失效 plist,`sys.exit(1)`
- 新增 `resolve_python_bin()`:優先專案 `.venv/bin/python`,退回 `sys.executable`,只回傳實際存在的路徑;兩者皆不存在 → 中止安裝並說明原因
- plist 改用 `.venv` Python(實測解析為 `~/Projects/digital-twin/.venv/bin/python`),`generate_plist_content(python_bin)` 接收經驗證的路徑
- `uninstall` 冪等化:plist 存在 → `launchctl unload -w` + 刪檔;plist 不存在但 launchctl 仍有 label → `launchctl bootout gui/uid/label` 清理殘留;兩者皆無才印「未發現服務」

## extract_feedback DB 路徑可設定

- `DB_PATH = Path(os.environ.get("OPENCODE_DB_PATH", 預設值))`;預設 `~/.local/share/opencode/opencode.db` 不變;實測 env 覆寫生效

## 驗收結果

| 驗收項目 | 結果 |
|---|---|
| install 驗證 BOT_SCRIPT 存在(不產失效 plist) | ✅ 單元測試覆蓋 |
| plist 用驗證過的 Python(優先 .venv) | ✅ 實測採用 .venv/bin/python |
| uninstall 冪等(plist 消失仍清理 launchctl 殘留) | ✅ 實測 bootout 清理 |
| OPENCODE_DB_PATH 覆寫,預設不變 | ✅ |
| extract_feedback --days 1 --only-corrections 正常執行 | ✅ Found 0 potential |

## 測試

- `tests/test_setup_daemon_paths.py` 4 tests
- 全量 `pytest -q`:**85 passed + 1 skipped**
- ruff(check+format)/pyright 0 errors;pre-commit 通過