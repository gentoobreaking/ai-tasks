# T054 任務完成摘要 — URL/環境變數常數收斂至 config（2026-08-12）

## 完成內容
1. **config.py 新增共用常數（env 可覆寫、附預設）**：
   - `TELEGRAM_SEND_URL`（預設 `https://api.telegram.org/bot{token}/sendMessage`，`{token}` 佔位符）
   - `OPENAI_API_URL`（預設 `https://api.openai.com/v1`）
   - `REDIS_URL`（預設 `redis://localhost:6379/0`）
   - `OPENROUTER_API_URL`（env 可覆寫；預設由 models.yaml nemotron 的 `api_base`
     + `/v1/chat/completions` 組合，避免雙源；config 內含 `_openrouter_api_base()` 退路）
2. **各檔改走 config 常數**：
   - `embedding.py`：OpenAI `EMBEDDING_BASE_URL` 預設退路 → `config.OPENAI_API_URL`；
     Ollama 硬編 `localhost:11434` → `config.OLLAMA_URL`（原硬編 3 處收斂）
   - `worker.py`：`REDIS_URL`、sendMessage URL → `config`
   - `telegram_bot.py`：`REDIS_URL` → `config`
   - `doctor.py`：`check_redis` 預設 → `config.REDIS_URL`
   - `discussion_orchestrator/orchestrator.py`：sendMessage URL → `config.TELEGRAM_SEND_URL`
   - `providers.py`：openrouter chat URL → `config.OPENROUTER_API_URL`
3. **行為不變**：env 值與現況預設一致時輸出相同；讀取時機（module import）不變

## 驗收
- [x] config.py 新增 OPENAI_API_URL / TELEGRAM_SEND_URL / REDIS_URL / OPENROUTER_API_URL（env 可覆寫附預設）
- [x] 各檔全改走 config 常數；`rg "api.openai.com|api.telegram.org|redis://localhost"`
      非 config/測試零殘留（scripts/*.sh 的 REDIS_URL shell 預設與 docs 註記為文字說明，保留）
- [x] 行為不變：telegram_bot/worker/doctor 測試維持通過
- [x] pytest 全量：182 passed + 1 skipped；ruff 全過；不引入新依賴

## 備註
- `auto_develop.py` 於 T051 已拆分，openrouter URL 現位於 `providers.py`，本任務收斂該處
- 已殘留且可接受的文字：`docs/deployment/telegram-bot.md` 的預設值說明、
  `scripts/*.sh` 的 REDIS_URL shell 預設、`docker-compose.prod.yml` 內的 setWebhook 註解範例
  （皆非 sendMessage/REDIS 常數的 Python 重複）
- commit：`2845b0b`（refactor）