# T050 任務完成摘要 — git 操作收斂 common/git.py（2026-08-11）

## 完成內容
1. **新增 `common/git.py`（單一收斂點，僅 stdlib）**
   - `git_diff_numstat(code_dir)`：`git add -A`（含未追蹤新檔）＋ `git diff --cached --numstat HEAD`，
     回傳 (added, removed, fname) 列——等價 build_pr_summary/count_diff_lines 原 add+diff 行為
   - `git_commit(code_dir, msg) -> hash|None`：add -A + commit + rev-parse（失敗回傳 None，
     保留「Git commit 失敗」告警）
   - `git_changed_files(code_dir) -> list[str]`：已追蹤修改（diff --name-only）＋未追蹤新檔
     （ls-files --others --exclude-standard）
   - `git_revert_all(code_dir, *, dry_run=False)`：破壞性還原（checkout -- . ＋ clean -fd），
     明確 docstring、dry_run 只告警不執行、tasks/config.py/logs 殘留容許語意一併收進
2. **auto_develop.py 五處全遷移**
   - `git_commit`（msg_type 判定保留，subprocess 移除）
   - `build_pr_summary`／`count_diff_lines` → `git_diff_numstat`（`_should_exclude` 過濾保留）
   - `_get_changed_files` → `git_changed_files`（.py 過濾保留）
   - `_git_revert_changes` → `git_revert_all` 薄委派（對外簽名不變）

## 驗證結果
- `rg 'subprocess.run(\["git"' auto_develop.py`：無殘留
- PR 摘要閘門語意不變：test_count_diff_lines（=6）、test_build_pr_summary_content 原樣通過
- pytest 全量：164 passed + 1 skipped（新增 3 項：git_changed_files／revert dry_run 防護／實際還原）
- ruff check 全過（ruff format 已套用）；pyright：12 errors（基線持平）

## 備註
- build_pr_summary 與 count_diff_lines 輸出語意各自保留（staging 時機與格式差異依存既定設計）
- 後續 T051（auto_develop 拆分）可直接以 common/git.py 為 git 層
- commit：`5f8f922`（refactor）