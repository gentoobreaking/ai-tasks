# T016 任務完成摘要（digital-twin）

- 日期：2026-08-06
- 專案：~/Projects/digital-twin（程式碼）
- 執行者：OpenCode with DeepSeek V4 Flash
- Git commit：ef892ca（fix(T016)）

## T016 - spec_auto_merge 移除假資料對照表 ✅

**問題**：`spec_auto_merge.py` 生成的 `05-merge-review.md` 對照表是**硬編碼的範例文字**（「錯誤處理 → ✅ 採用 DeepSeek」等），並非從模型輸出實際分析得出，會誤導人工審查流程。

**實作**（`spec_auto_merge.py`）：
- 移除寫死的 3 行假對照表（整體架構/錯誤處理/資料流程）
- 新增 `_extract_sections()`：從各模型輸出實際提取 `##`/`###` 章節標題，自動生成「模型章節覆蓋對照表」（✅ 有該章節 / — 無）
- 決策欄一律「⚠️ 需人工填寫」，不自動預設採用任何模型
- 無法提取章節時 fallback：對照表只留標題行 + 「⚠️ 需人工填寫」註記（任務書最小可行方案）
- 保留「各模型章節摘錄」區塊（真實輸出）
- `merge-decision.md` 範本同步移除預填決策（整體架構/介面驗證/錯誤日誌 3 行假決策 + Grok 風險假資料）
- 修正標題 `vv2` 重複前綴 bug（版本號正規化後不再拼接 `v{version}`）
- `ruff check spec_auto_merge.py` 全數通過（含既有 E501 長行）

**驗證（逐項）**：
- ✅ `python3 spec_auto_merge.py --project digital-twin --version v2`（接受 v2 與 2 兩種格式）→ 產出無任何硬編碼決策
- ✅ `05-merge-review.md`：35 個章節對照全部標「⚠️ 需人工填寫」，無「採用 X」結論
- ✅ v3 同步重新生成（26 章節對照）
- ✅ grep 確認「採用 DeepSeek/Gemini/Claude」「🔀 融合」「Zod/Pydantic」「極簡無過度設計」零殘留（命中皆為模型輸出真實摘錄）
- ✅ `merge-decision.md` 僅含「⚠️ 需人工填寫」佔位
- ✅ pytest 5 passed 不受影響

## 檔案變更
- `spec_auto_merge.py`：`_extract_sections()`、章節覆蓋對照、決策欄「需人工填寫」、merge-decision 範本淨化、版本號 bug 修正
- `specs/ai-consultations/v2/05-merge-review.md`、`v2/merge-decision.md`、`v3/05-merge-review.md`、`v3/merge-decision.md`：重新生成（無假資料）
- 任務書：`tasks/T016-spec-merge-no-fake-data.md` → done + 驗收 checkbox 全勾

## 待辦
- T018（任務依賴欄位）→ T019（PR 摘要閘門）→ 其餘
