# T010 任務完成摘要 — Agent SemVer + Canary Deploy + Rollback

- **狀態**: done
- **Commit**: 105f0bc
- **日期**: 2026-08-07

## 交付內容

1. **`agent_versioning.py`(新增)**
   - SemVer front-matter 強制:agent_version / last_updated / change_log / rollback_to
   - `prompts/{agent}/` 版本化目錄 + `current` / `staging` symlink
   - `init`(初始化全部 4 個 agent 至 v1.0.0)
   - `generate-rc`:產生 `v{ver}-rc.{n}.md`,不碰 production
   - `canary deploy|promote|rollback`:
     - deploy:staging symlink 指向指定版本
     - promote:RC→穩定版號,current 更新,同步 agent 檔,rollback_to 設為前一版
     - rollback:回退至 rollback_to 或指定版本,同步 agent 檔
   - `status`:檢視各 agent 版本狀態

2. **`apply_feedback.py` 整合** --dry-run / --generate-rc / --promote / --rollback

3. **`twin` CLI**:`twin canary <agent> <version> --action deploy|promote|rollback`

4. **4 個 agent 檔**初始化 SemVer front-matter(agent_version: 1.0.0)

## 驗收結果

| 驗收項目 | 結果 |
|---|---|
| `apply_feedback.py --generate-rc` 產生 rc 版本 | ✅ dry-run 產生 v1.1.0-rc.2 預覽;既有 v1.1.0-rc.1 |
| `twin canary deploy/promote/rollback` 正常運作 | ✅ 實機走完整流程(deploy→promote→rollback) |
| `current` symlink 正確指向穩定版 | ✅ rollback 後回 v1.0.0.md |

## 測試

- `tests/test_agent_versioning.py`:11 tests 全通過
- 全套件:60 passed + 1 skipped