# T034 任務完成摘要 — Routing Rules 微調與分身同步 Registry（2026-08-09）

## 完成內容
1. **agent_registry.py `extract_tags` 修正**：fallback 只掃描 frontmatter 之後的正文，避免「取任務檔 YAML 欄位名（`summary`/`status`/`priority`/`depends_on` 等）」被當成標籤，誤觸 routing keywords。「T002 `summary` 觸發 docs-sync」的誤判正源自此（T001/T002/T006/T008/T009/T010 等 7 檔皆中招）。
2. **`.opencode/agent_registry.yaml` keywords 微調**：移除 rule-cloud 的 `deploy`（T010 agent-versioning 內文含 canary deploy → 誤判 cloud）、rule-docs 的 `readme`（T011 unified-config 內文引 README → 誤判 docs）。
3. **三分身同步引用 Registry**：cloud-arch-clone / docs-sync-clone / quant-dev-clone 新增「SOP 4.1 任務啟動與委派」流程（`./twin route --task-id --auto` 查 `delegated_to`；`.opencode/agent_registry.yaml` 定義），並依 T010 慣例版本升至 v1.0.1（change_log 記錄 T034）。
4. **README §1**：新增任務委派 Registry 說明（呼叫路由、routing.json 回寫、三分身 SOP 4.1 引用）。

## 驗證
- 35 個任務檔路由分佈：my=30、cloud-arch=2（T004 dockerfile、T032 telegram 部署）、docs-sync=3（T016 spec-merge、T023 blocked review、T034 自身），quant-dev=0（digital-twin 專案無量化任務，規則保留給未來）。
- `test_agent_registry.py` 10 passed；全量 pytest 115 passed（1 skipped 網路相關）。
- CLI 實測 T002 → my，routing.json 正確回寫。

## 影響
- `agent_registry.py` 的 extract_tags 行為已改（不再含 frontmatter 欄位名）——與 T008 原始定型有差異，T035 向下相容時認列。

## 下一步
- T035：twin route CLI 擴充（--list-agents / --show-rules / --dry-run）