# T011 + T012 完成報告（含 T024 順手修復）

- 時間：2026-08-05
- 任務書位置：`~/Projects/digital-twin/tasks/`

## 完成內容

### T011 - 統一模型與路徑設定（done）
- 新增 `config.py`（4097 bytes）：projects 清單、模型常數（nemotron/gemini/deepseek/grok 各含 api_env/enabled/manual）、DEFAULT_IMPL_MODEL
- 修改 `multi_ai_discuss.py`（2 blocks）、`auto_develop.py`（移除 L605-607 重複定義）、`consensus_eval.py`（改用 config MODELS）、`spec_auto_merge.py`（MODEL_NAMES 改 nemotron 等）
- 驗證：`--list-models` 顯示 nemotron/gemini/deepseek ✅、grok ⚠️ 手動；`auto_develop.py --list` 正常

### T012 - 品質閘門分層（done）
- `run_tests(code_dir, changed_files)`：L1 ruff 只查 diff 檔案（無變更時退路只查 E9/F821 致命錯誤）、L2 pyright、L3 pytest（rc=5 明確警告）
- 新增 `_get_changed_files()`：git diff --name-only + git ls-files --others，只留 .py
- 實測：語法錯誤擋下 ✅、乾淨檔案通過 ✅、全專案 333 舊債不再誤擋 ✅

### T024 - twin Python 解析器（done，順手修復）
- 問題：`twin` 用 `sys.executable`（QClaw python 無 dotenv）→ `twin status` 顯示「無 Key」假象
- 修正：`_resolve_python()` 依序嘗試有依賴的直譯器；`twin status` 現在正確顯示 3 ✅
- 附帶：`spec_auto_merge.py` version 前綴 bug（vv2）修正、review 表頭 Claude→Nemotron

## 驗證結果
- `/opt/homebrew/bin/python3 -m py_compile` 全部檔案通過
- `./twin status`、`./twin merge digital-twin v2`、`python3 auto_develop.py --list` 均正常

## 待辦
- T013（失敗還原工作目錄）、T014（自動修復迴圈）未動，待執行
- T017（pyright include 指向 src/）、T015（README 模型表對齊）待執行
- 任務完成摘要 commit 到 free-ai-router 尚待執行（依 SOP）
