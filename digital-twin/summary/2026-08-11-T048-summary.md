# T048 任務完成摘要 — TaskStore.update_fields 唯一重寫積木（2026-08-11）

## 完成內容
1. **common/tasks.py**：
   - 新增 `TaskStore.update_fields(task, *, force=False, **fields) -> bool`：
     - 一律刷新 `updated` 日期（與既有 5 處行為一致）
     - 保留 `yaml.dump(allow_unicode=True, sort_keys=False)` 欄位順序語意（不重排）
     - T037 防護：`force=False`（預設）時，frontmatter 帶 commit 的已完成任務拒絕被降級回
       in-progress/pending（guard 訊息原樣）；`force=True` 供 retry/supersede 人工決策管道繞過
   - `save_summary`/`set_status` 改為委派 `update_fields`（行為不變，程式碼單一化）
2. **auto_develop.py** 4 處重寫塊全遷移：
   - `generate_blocked_review`：以磁碟最新 frontmatter 的 Task（T023 語義）呼叫 update_fields 寫 `blocked_review`
   - `retry_task`：`force=True, status=pending, fail_count=0, summary=…`
   - `supersede_task`：`force=True, superseded_by=T0XX, status=superseded, summary=…`
   - `_record_failure`（未達上限路徑）：`status=pending, fail_count=N, summary=…`
   - 移除模組層 `import yaml`（F401；函式內局部 import 維持）

## 驗證結果
- `rg 'yaml.dump(fm' auto_develop.py`：無殘留（重寫積木僅存 TaskStore）
- pytest 全量：151 passed + 1 skipped
- ruff check：全過；pyright：12 errors（基線持平）
- 無新依賴

## 備註
- T037 防護行為驗證：test_doctor 的 done+commit 拒絕降級測試、test_blocked_review 的
  retry/supersede/blocked review 測試全數維持通過
- commit：`9dab005`