# T047 任務完成摘要 — 任務檔 frontmatter 解析全面收斂 TaskStore（2026-08-11）

## 完成內容
1. **common/tasks.py**（唯一實作）：
   - 新增 `parse_frontmatter(text)`：無 frontmatter → `({}, 原 text)`；YAML 解析失敗或非 dict →
     `({}, body 保留)`。語義等價於 agent_versioning 原 regex 版本，且對壞 YAML 不再向外拋例外
   - 新增 `dump_frontmatter(meta, body)`：`safe_dump(default_flow_style=False)` + body 前空白處理
   - `parse_task_file` 改用 `parse_frontmatter`（移除自身 split 手寫解析）
2. **agent_versioning.py**：
   - 刪除 `FM_RE` 與本地 `parse_frontmatter`/`dump_frontmatter`（原 :91/103），
     改 `from common.tasks import dump_frontmatter, parse_frontmatter`（12+ 使用處不變）
   - 刪除 `import yaml`（該檔不再直接使用 YAML）
3. **doctor.py** `check_tasks_integrity`：刪除 `text.split("---", 2)[1]` 手寫解析（:291），
   改用 `common.tasks.parse_frontmatter`（無 frontmatter / 解析失敗 / 非 dict 皆跳過，行為等價）；
   移除局部 `import yaml`
4. **incremental_index.py** `_frontmatter_tags`：改 `common.tasks.parse_frontmatter` 解析，
   保留原 tags 字串/清單兩種展開語義；移除局部 `import yaml`

## 驗證結果
- `rg 'split("---"|FM_RE'`：任務檔解析僅剩 `common/tasks.py` 一處
- pytest 全量：151 passed + 1 skipped（原基線）
- ruff check：全過；pyright：12 errors（與既有基線持平）
- 無新依賴（doctor / incremental_index 反而移除局部 yaml import）

## 備註
- doctor 驗收原文「改用 parse_task_file」實作以 `parse_frontmatter` 達成：完整性檢查
  只需 commit/status 欄位，且任務檔可能非 T### 命名（parse_task_file 對非規範檔名回傳 None）
- **留待 T048**：auto_develop.py（4 處）與 TaskStore.rewrite 的 `yaml.dump` 重寫積木統一
  （本任務範圍為「解析」收斂；重寫統一為 T048「TaskStore 重寫積木統一」）
- commit：`b8255a6`（refactor）＋ `docs` commit（README 註記）