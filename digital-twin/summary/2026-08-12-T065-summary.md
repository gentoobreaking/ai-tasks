# T065 Summary — providers build_implementation_prompt 改用實際任務參數

## 完成內容
- `providers.py:build_implementation_prompt` 不再硬編碼 `Project: digital-twin` / `Code path: ~/Projects/digital-twin/`
- Project 名稱來自 `project` 參數；Code path 由 `PROJECT_PATHS[project].code_dir` 動態取得（digital-twin 無 entry 時保留原預設路徑以向後相容）
- 先前被忽略的 `spec` / `codebase` 參數現在實際寫入 prompt（Codebase context / Spec 章節）

## 驗證
- 新增 `tests/test_impl_prompt_params.py`（4 tests）：
  - 非 digital-twin 專案名/路徑出現在 prompt 且無 hardcode
  - code_dir 動態取自 PROJECT_PATHS（tmp_path）
  - PROJECT_PATHS 無此專案時不輸出錯誤路徑
  - digital-twin legacy 預設路徑保留
- pytest 全量：237 passed + 1 skipped（+4）
- ruff check / ruff format 全過

## 影響檔案
- `providers.py`（build_implementation_prompt）
- `tests/test_impl_prompt_params.py`（新增）