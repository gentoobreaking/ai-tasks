# T005 Summary: 統一結構化日誌與 OpenTelemetry

- 日期: 2026-08-06
- Commit: `ddddf62`
- 狀態: done

## 交付內容

### 1. `common/observability.py`（核心交付）
| 功能 | 說明 |
|------|------|
| `init_otel(service_name)` | 初始化 OTEL:ConsoleSpanExporter(console trace)+ PrometheusMetricReader + `/metrics` HTTP(預設 9464,`OTEL_METRICS_PORT` 可覆寫,0=關閉) |
| structlog JSON stdout | level/timestamp/logger 欄位,第三方日誌走 stderr 避免汙染 JSON 流 |
| `get_logger()` | 各腳本統一獲取 JSON logger |
| `get_meter()` | OTEL meter(降級時 None) |
| 指標建構 | discussion_round_duration / rag_query_latency / feedback_apply_duration / tg_command_duration + `record_duration()` 安全記錄 |

### 2. 植入腳本
| 腳本 | 埋點 | 指標 |
|------|------|------|
| multi_ai_discuss.py | run() 每輪每模型計時 + token 估算(1 token≈4 字元) | `discussion_round_duration_seconds{model,round}` |
| index_knowledge.py | search_knowledge_base 計時 | `rag_query_latency_seconds{version:v1}` |
| apply_feedback.py | apply_feedback_item 計時 | `feedback_apply_duration_seconds{agent,version:v1}` |
| telegram_bot.py | ❌ 不存在(T006 失敗)- 指標建構已預留 | `tg_command_duration_seconds` 待 T006 |

### 3. 工程
- pyproject.toml:dependencies/prod/dev 補 opentelemetry-sdk/api/exporter-prometheus + prometheus-client
- Dockerfile:builder 補 OTEL 依賴、COPY common/、HEALTHCHECK 納入 common.observability

## 驗證
- pytest 5 passed
- 三腳本實跑 JSON 日誌正常(含 metrics server 啟動訊息)
- /metrics 實抓:`rag_query_latency_seconds_count{version="v1"} 1.0` / `_sum 0.14`
- 降級模式(系統 python 無 OTEL):警告 + 純 structlog JSON,正常運行
- /metrics port 衝突(三腳本並發同 port 9464):降級警告不崩潰
- ruff:新增錯誤 0(既有 23 個未動);import 排序已自動修

## 設計決策
1. **OTEL 可選依賴**:import 失敗靜默降級,保證所有腳本在任何環境可用
2. **降級優先**:metrics server 啟動失敗僅警告,不阻斷主流程
3. **token 估算**:模型 API 未回傳 usage 欄位,以 len(output)/4 估算,已於 log 標明「估算」

## 後續
- T006 重啟後於 telegram_bot.py 植入 tg_command_duration_seconds{command,role} + 錯誤計數