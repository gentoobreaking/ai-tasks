# T040 任務完成摘要 — MODELS 模型清單 YAML 配置（2026-08-09）

## 完成內容
1. **`.opencode/models.yaml`**（新增）：nemotron/gemini/deepseek/grok 4 模型（grok 維持
   `enabled=false` + `manual=true`）；檔案存在即為 MODELS 唯一事實來源（整份取代）
2. **`config.py`**：
   - `load_models_from_yaml(path)`：欄位容缺（name/role/api_env/model_id 必填，其餘走預設：
     api_base=""、provider=openai、enabled=true、manual=false）；壞 YAML／空清單／不存在 → None（沿用內建）
   - 匯入時 `if (loaded := load_models_from_yaml()) is not None: MODELS = loaded`，
     `get_model()`/`MODEL_NAMES` 引用不變；沿用 T039 的 yaml→fallback 模式
3. **測試**：`tests/test_models_yaml.py` 6 項離線測試
4. **文件**：README §2 補「模型清單可經 .opencode/models.yaml 配置」

## 驗證
- `./twin status` 與 `multi_ai_discuss.py --list-models` 與內建階段輸出一致（YAML 已生效）
- 6 測試：內建 fallback／YAML 覆蓋+disabled+缺省／缺必填跳過／壞 YAML／空清單／repo YAML 與 MODELS 等價
- 全量 134 passed + 1 skipped（128 → 134）；ruff check/format 全過
- 程式碼 commit `3f5e4b8`（含變更摘要）

## 影響
- 新增/停用模型今後僅需改 YAML，不動程式碼
- YAML 損壞時平滑退回內建（有警示訊息印出）