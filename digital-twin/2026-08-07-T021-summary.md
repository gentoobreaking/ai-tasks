# T021 任務完成摘要 — gen_mermaid 真實掃描 + consensus 中文分詞

- **狀態**: done
- **Commit**: d4e7763
- **日期**: 2026-08-07

## gen_mermaid 真實掃描化(原本是假圖生成器)

- `--project` 經 `PROJECT_PATHS[project]["code_dir"]`(缺檔時退回 `~/Projects/<name>`)解析實際路徑
- 掃描專案內所有 `*.py`,排除 `__pycache__/.git/.venv/node_modules/logs/.opencode` 等
- 模組/檔案層級架構圖:頂層目錄 → subgraph,檔案為節點;`from/import` 抽算子模組建立 group 層級邊(真實模組圖)
- 節點上限 `MAX_NODES=60` 防爆;掃不到時明確報錯「僅輸出真實掃描結果,不產生假圖」並回傳 1
- `--flow` 以實際開發閉環(任務→實作→修復→品質閘門→commit→更新任務書)為 sequenceDiagram 腳本
- positional 專案名(相容 `./twin draw <project>`)
- 額外修正 `./twin` `_resolve_python()`:優先專案 `.venv`(原 homebrew python 缺 tenacity,`twin consensus` 直接 import 崩潰)

## consensus 中文分詞 + 去魔法數字

- `_tokenize()`:英文/數字單字照舊 + **中文字元 bigram**(不依賴 jieba),解決中文整句被當 1 token 失真
- 移除 `base * 3.5 + 0.35` magic,直接回傳平均 Jaccard(0.0~1.0),加註說明
- 實測:相同 1.0、部分重疊 0.56、完全不同 0.0;真實 3 模型測試得到 0.14(低共識),舊式會放大到 0.84 誤判為高共識

## 驗收結果

| 驗收項目 | 結果 |
|---|---|
| --project 真實掃描,排除噪音目錄 | ✅ |
| 掃描不到時明確提示 | ✅ FileNotFoundError |
| --flow 實際流程時序圖 | ✅ 開發閉環 |
| 中文分詞改善(不新增重依賴) | ✅ 字元級 bigram |
| 移除 magic,改平均 Jaccard | ✅ |
| ./twin draw / ./twin consensus 可執行 | ✅ 後者 3 模型實跑 |

## 測試

- `tests/test_gen_mermaid_consensus.py`:7 tests(掃描真實檔案/排除噪音/路徑不存在 raise/無 py raise / bigram / 相似度單調 / magic 已移除)
- 全量:`pytest -q` **81 passed + 1 skipped**
- ruff / pyright 0 errors;pre-commit 全部通過