# T035 任務完成摘要 — twin route CLI 擴充（2026-08-09）

## 完成內容
1. **agent_registry.py route 子命令新增 3 旗標**（T008 基本版擴充，twin CLI 透傳自動支援）：
   - `--list-agents`：僅輸出所有 agents（id、role、model、capabilities 摘要），不做路由
   - `--show-rules`：僅輸出所有 routing rules（id、delegate_to、keywords 摘要），不做路由
   - `--dry-run`：路由結果照常輸出並顯示「匹配 rule」，但不寫入 `{任務檔}.routing.json`；`--tags` 與 `--task-id --auto` 兩種模式都支援
2. **重構**：抽出 `print_agents()` / `print_rules()` 供 `route` 與既有 `list` 子命令共用（去重複）。
3. **twin 腳本**：help 區塊補 3 行 T035 範例；README §1 補除錯旗標說明。

## 驗證（逐項）
- `./twin route --list-agents` → 輸出 4 分身 id/role/model/capabilities，exit 0
- `./twin route --show-rules` → 輸出 rule-quant/cloud/docs/default + keywords 摘要
- `./twin route --tags kubernetes,docker --dry-run` → cloud-arch + rule-cloud，未產生檔案
- `./twin route --task-id T002 --auto --dry-run` → my + rule-default，⚡ dry-run 提示，無 routing.json
- 回寫模式回歸：實機再跑一次 `--task-id T002 --auto`（無 dry-run）確認 routing.json 正常產生

## 測試
- `test_agent_registry.py` 10 → 14 passed（新增 `--list-agents`、`--show-rules`、`--tags --dry-run`、`--task-id --auto --dry-run` 4 項，含 dry-run 不寫檔斷言）
- 全量 pytest：119 passed + 1 skipped（網路相關）

## 影響
- 既有 `twin route` / `agent_registry.py route` 行為不變（新旗標皆選用）
- `list` 子命令輸出排版維持一致（共用新 helper）