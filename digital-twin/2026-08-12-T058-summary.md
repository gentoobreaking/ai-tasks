# T058 任務完成摘要 — dotenv 選用載入收斂（2026-08-12）

## 完成內容
1. **config.py 成為 .env 唯一載入點（T058）**
   - 新增 `ensure_dotenv()`：冪等（`_DOTENV_LOADED` 旗標，僅首次真正 load_dotenv）、
     無 dotenv 套件時靜默略過
   - config 於 import 時即呼叫一次，作為單一來源
2. **移除 6 個模組的重複載入**（原 9 處 try/load_dotenv 模式）：
   - worker.py：改 `from config import REDIS_URL`（import 即載入）
   - telegram_bot.py / auto_develop.py / doctor.py / embedding.py：
     移除 try 區塊，依賴既有 `import config` / `from config import`
   - consensus_eval.py：經 multi_ai_discuss → config 連鎖載入（註明觸發）
3. **rg 驗證**：非 config.py 已無 `load_dotenv`（見驗收）

## 驗收
- [x] 各腳本保留獨立執行能力——config 無循環依賴（只 import stdlib＋選用 yaml/dotenv），
  每個腳本都經 `import config` 觸發單次載入即可取得 .env
- [x] `rg "load_dotenv"`：僅 config.py 殘留（定義＋呼叫，符合「config 單一來源」預期）
- [x] telegram_bot / worker / doctor 可獨立執行且 .env env 仍生效：
  逐一 `python -c "import <模組>"` 成功，並驗證 .env 中 ANTHROPIC/GEMINI/DEEPSEEK/
  OPENROUTER_API_KEY 均 SET（經 config 載入）
- [x] pytest 全量 192 passed + 1 skipped；ruff 全過

## 備註
- telegram_bot 原三處選用載入（:31/:100/:193）已於先前 refactor 收斂為單一頂層，
  本次一併納入移除；lazy import 語意未受影響
- ensure_dotenv() 保持外部可呼叫（供少數不需 import config 的腳本顯式使用）
- commit：`dbfa18c`（refactor）