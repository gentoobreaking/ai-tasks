# T033 任務完成摘要 — spec_auto_merge 整合 DiscussionOrchestrator 狀態機

- **狀態**: done
- **Commit**: 560542f
- **日期**: 2026-08-08

## 交付

### discussion_orchestrator/orchestrator.py
- 新增 `DiscussionOrchestrator.load(project, version, out_dir=None)` classmethod：
  - 從既有 manifest.json 重建狀態（status / rounds / token_budget / tokens_used / all_outputs）
  - 不建立 model adapter（純狀態操作，不需 API key；`adapters={}`）
  - 保留原始 manifest 快照（`_manifest_snapshot`）
- `save_manifest()` 快照模式：只更新 `status` / `tokens_used` / `updated`，不覆寫討論資料（models / outputs）
- manifest 不存在拋 `FileNotFoundError`（含明確訊息與操作提示）

### spec_auto_merge.py
- `_advance_state(project, version, to_status, out_dir=None)`：
  - `load()` → `to_merge()`（MERGE）或 `archive()`（ARCHIVED）
  - 無 manifest 的舊版目錄（v2/v3）提示跳過，不阻斷檔案生成
  - 非法遷移（如 INIT → MERGE、HUMAN_REVIEW → ARCHIVED）拋 StateError
- `generate_spec_merge()` 生成 05-merge-review.md / merge-decision.md 後自動推進至 MERGE
- 新增 `--archive` 旗標：歸檔流程（MERGE → ARCHIVED）

### twin CLI
- 新增 `./twin archive <proj> <ver>`；`merge` 說明更新為「自動推進 MERGE」

## 驗收結果

| 驗收項目 | 結果 |
|---|---|
| 合併完成後呼叫 to_merge() | ✅ generate_spec_merge 結尾自動推進 |
| 歸檔流程呼叫 archive() | ✅ `--archive` / `./twin archive` |
| manifest status 遷移 | ✅ HUMAN_REVIEW → MERGE → ARCHIVED 實測 |
| CLI 整合 | ✅ `./twin merge`（內部）＋ 新增 `./twin archive` |
| 測試 | ✅ merge 後 MERGE、archive 後 ARCHIVED |

## 測試

- `tests/test_spec_auto_merge.py` 6 tests：
  1. merge 後 manifest status = MERGE
  2. archive 後 status = ARCHIVED（先 merge）
  3. 保留討論資料（outputs/models/tokens_used 不被覆寫）
  4. 無 manifest → None（跳過不崩潰）
  5. 非法遷移（INIT → MERGE）→ StateError
  6. generate_spec_merge 產生檔案且狀態推進至 MERGE
- 全量 `pytest -q`：**115 passed + 1 skipped**
- CLI 實測：`./twin archive` 對無 manifest 目錄正確提示跳過