# T032 任務完成摘要 — Telegram Bot 生產部署文件與啟動腳本

- **狀態**: done
- **Commit**: e04b651
- **日期**: 2026-08-09

## 交付

### docs/deployment/telegram-bot.md（新）
- 架構圖（Webhook → FastAPI → Redis Stream → Worker Pool → 通知回傳）、環境變數表、啟動命令
- Redis 前置檢查 / launchd / Docker Compose 部署、set_webhook 註冊步驟與驗證（getWebhookInfo）、health check、疑難排解對照表

### scripts/start-telegram-bot.sh（新，chmod +x）
- start / status / stop / restart；PID 管理（logs/pids/）；日誌輸出 logs/telegram-bot.log、logs/telegram-worker.log
- Redis 連線預檢（非致命提示）；`--workers N` 參數校驗；啟動失敗即時偵測
- bash 3.2 相容性修正：`$name（` 形態（變數後緊接 CJK 全型字元）會被 Apple bash 併入變數名導致 unbound variable → 全面改用 `${name}`/`${pid}` 明確邊界

### scripts/set-tele-webhook.sh（新，chmod +x）
- 讀 .env / 環境變數，`setWebhook` 指向公開網址（自動補 `/api/webhook`），`getWebhookInfo` 驗證

### docker-compose.prod.yml（新）
- telegram-bot + worker + redis：healthcheck（/healthz、redis ping）、depends_on condition healthy、AOF 持久化

### telegram_bot.py（修正）
- `main()` 改為同步執行 `uvicorn.run()`（原 `asyncio.run(main())` 內部再跑 uvicorn 在 Python 3.14 直接崩潰「asyncio.run cannot be called from running event loop」）；worker 分支才用 asyncio.run

### README / .env.example / twin
- README §6 改為「✅ 已實作」部署章節並連結文件；架構圖、§5 樞紐、目錄樹移除「未實作」過時註記
- `.env.example` 補 `WEBHOOK_URL`；`./twin bot` 訊息更新（指向部署文件）

## 驗收結果

| 驗收項目 | 結果 |
|---|---|
| 部署文件 | ✅ docs/deployment/telegram-bot.md（含架構/環境變數/health check） |
| 雙程序啟動腳本 | ✅ 實測 start/status/healthz/stop 全流程 |
| docker-compose.prod.yml | ✅ YAML validation 通過 |
| Webhook 設定文件/腳本 | ✅ set-tele-webhook.sh + 文件步驟（bot.set_webhook 指向 /api/webhook） |
| README 部署章節 | ✅ §6 連結至部署文件 |

## 測試

- 腳本 end-to-end：start → status（雙程序）→ `curl /healthz` → stop → status（均未執行）+ 無殘留程序
- 全量 `pytest -q`：115 passed + 1 skipped（tests/test_telegram_bot.py 19 passed）
- bash -n 語法檢查；docker-compose YAML 解析驗證