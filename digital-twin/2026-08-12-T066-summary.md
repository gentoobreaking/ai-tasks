# T066 Summary — diff _normalize_path 路徑穿越 containment 防護

## 完成內容
- `diff.py:_normalize_path` 加入 containment 檢查：解析後路徑必須 `is_relative_to(code_dir.resolve())`，逃逸（mid-path `..` 穿越）→ 回傳空字串
- `scheduler.py` 兩個 dict-write 迴路（:778-782 / :855-859）加入「空路徑跳過」防護，越界路徑不會寫入 project 之外、不中斷任務
- apply_diff 路徑經由 `git apply` 本身於 repo worktree 內限制，逃逸路徑 → patch-invalid → apply 失敗 → task failure，仍安全

## 驗證
- 新增 `tests/test_diff_path_containment.py`（9 tests）：
  - 正常相對路徑不變
  - `/dev/null` 保留
  - 絕對路徑指向 code_dir 內 → 正規化為相對
  - mid-path `foo/../../etc/passwd` → `""` 被拒
  - 純前導 `..`/`/etc/...` 因 strip 成相對路徑寫入 code_dir 內（安全，不逃逸）
  - scheduler dict-write loop：越界路徑 `applied == 0` 且未在 project 外創檔
  - scheduler 匯入之 `_normalize_path` 行為一致
- pytest 全量：246 passed + 1 skipped（+9）；ruff check/format 全過

## 影響檔案
- diff.py（_normalize_path）
- scheduler.py（process_task 兩處 dict-write）
- tests/test_diff_path_containment.py（新增）