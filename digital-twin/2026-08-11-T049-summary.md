# T049 任務完成摘要 — 完成流程單軌化＋一致性檢查（2026-08-11）

## 完成內容
1. **sync_readme 單軌化（auto 與手動共用）**
   - `auto_develop.sync_readme` 回傳值改為可辨識狀態碼：`created`（無 README 建立標準表格）／
     `updated`（有標準表格已更新）／`skipped`（非標準表格不覆寫）／`error`（專案不存在）
   - 新增 CLI：`auto_develop.py --sync-readme [--project X]`（exit：created/updated=0，其餘=1）
   - `twin` 新增子命令：`./twin sync-readme [--project X]`
   - `.opencode/commands/complete-task.md` 步驟 4 改為執行 `./twin sync-readme --project {專案}`
     （手動完成路徑與 auto 的 process_task 使用同一函式 → 輸出一致）
2. **doctor 一致性檢查（WARN 級，接進 run_all）**
   - README 標準任務表（`| 編號 | 名稱 | 狀態 | 優先級`）逐列對照任務檔：
     存在性（README 列出但檔案不存在）與狀態（README vs frontmatter）不符 → WARN；
     無標準表格（人工維護風格）→ info 跳過不誤報
   - specs：掃描 `{tasks_base}/specs/*/merge-decision.md` 與 `{code_dir}/specs/*/*/merge-decision.md`，
     取最新版本；任務檔 frontmatter 無任何 spec 對照欄位（spec/spec_version/spec_dir…）→ WARN

## 驗證結果
- `./twin sync-readme --project digital-twin` → skipped（人工風格 README 原樣保留，未覆寫）
- doctor 真實環境：digital-twin README→info 跳過；specs v3 缺對照欄位→WARN（符合設計）
- pytest 全量：161 passed + 1 skipped（含新增 10 項測試：一致性 6＋sync_readme 4）
- ruff check：全過（ruff format 已套用）；pyright：12 errors（基線持平）

## 備註
- 一致性檢查維持 WARN 級（不變 FAIL），避免環境差異誤報（依任務備註）
- commit：`8f84670`（feature）＋ docs commit（README 註記）