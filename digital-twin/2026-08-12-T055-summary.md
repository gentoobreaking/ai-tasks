# T055 任務完成摘要 — twin CLI 子命令 --help 可達（2026-08-12）

## 完成內容
1. **方案 A：子命令 --help 直通被轉發腳本 argparse**：
   - `twin` 新增 `_CMD_TARGET`（20 個子命令 → 對應腳本）對應表
   - `main()` 在分派前先攔截 args 含 `-h`/`--help` → 以子程序呼叫 `python <腳本> --help`
     （在 twin 層先行攔截，避免 target 腳本自己的 syntax 預檢查吃掉 --help），顯示後 exit 0
   - `projects`（無被轉發腳本）特例輸出自身用法
   - 逐一驗證 20 個子命令 `--help` 皆 exit 0
2. **`twin bot stop` 語意釐清**：
   - 原對映 `setup_daemon.py uninstall`（等效移除服務、取消登入自啟，語意怪異）
   - 改為 `setup_daemon.py stop`：新增 `stop_daemon()`（`launchctl bootout gui/<uid>/<label>`）
     只停當前執行實例、保留 plist（登入仍自啟）
   - `uninstall` 保留為獨立動作；bot 語法改為 `[install|status|start|stop|uninstall]`
   - `setup_daemon.py` 亦新增 `--help` 與 `stop` 動作
3. **文件同步**：`docs/deployment/telegram-bot.md` 新增 launchd 三動作與
   「stop ≠ uninstall」說明

## 驗收
- [x] 20 個子命令皆支援 `--help`（方案 A：`python <script> --help` 直通），exit 0 不觸發實際執行
- [x] `twin --help` / `twin help` 維持總覽輸出
- [x] `twin bot stop` 語意釐清（stop＝暫時停止、保留 plist；uninstall＝移除）；文件同步說明差異
- [x] 既有命令行為不變（twin status / projects / bot status 正常；test_telegram_bot 等通過）
- [x] pytest 全量：182 passed + 1 skipped；ruff 全過；shellcheck 環境未安裝（無新增警告可驗證）

## 備註
- 未知命令 + `--help`（如 `twin rust --help`）維持原「未知命令」輸出，不回針對 --help
- 未新增測試檔（twin 為無副檔名腳本，既有測試沿用）；如需可在後續補 CLI 整合測試
- commit：`d499432`（feature）