# T006 Summary: Telegram Bot Webhook 重構

- 日期: 2026-08-06
- Commit: `fde2d86`
- 狀態: done（先前失敗 3 次後重啟成功）

## 核心：解耦架構（針對先失敗根因）
| 傳統（失敗） | 新架構（本次） |
|---|---|
| 單進程輪詢（polling）阻塞主循環 | aiogram 3 Webhook + FastAPI 立即回應 |
| webhook 進程內嵌呼叫 AI 模型（導致卡死失敗） | 任務入 Redis Stream，Worker 背景 subprocess 執行 multi_ai_discuss.py |

## 交付檔案
| 檔案 | 重點 |
|---|---|
| `telegram_bot.py` | FastAPI `/api/webhook` `/healthz` `/metrics`；aiogram Dispatcher；RBAC(admin/operator/viewer)；sanitize_input 消毒；入隊即回「已排入佇列」；`tg_command_duration_seconds{command,role}` |
| `worker.py` | Worker Pool(預設 4)消費 Redis Stream；discuss→subprocess 背景執行；RAG 計時；pybreaker 熔斷(失敗 5 次開路 60s)；完成 push 通知 |
| `sanitize_input.py` | 注入/系統指令/路徑穿越/下載執行→拒絕；PII(email/電話/身分證/信用卡/IP)→遮罩 |
| `tests/test_telegram_bot.py` | 25 個測試(sanitize/RBAC/Redis queue/endpoints/worker/breaker) |
| pyproject/Dockerfile | 補 aiogram/fastapi/uvicorn/redis/pybreaker(telegram extra 取代 python-telegram-bot) |
| docker-compose | redis 補 `ports: 6379`(本機/容器存取) |
| .env.example | 補 RBAC/Redis/熔斷/OTEL 變數 |

## 驗證
- `pytest: 25 passed`(breaker 開路/重置、RBAC 矩陣、sanitize 規則、Endpoints、Redis 佇列)
- 端到端(docker redis): enqueue → stream → Worker Pool 消費 → ack ✓
- uvicorn 實測:`/healthz {"status":"ok"}`、`/metrics` 4 HELP 系列、`/api/webhook` 接受 payload
- 熔斷:6 次 failure → `is_open=True`;success → 重置
- ruff 全過(既有 E501 23 個未增加)

## 設計決策
1. **pybreaker 2.x 相容**:state.call 僅同步;`AIBreaker` 包裝以 `fail_counter ≥ fail_max` 開路、`_handle_success()` 重置
2. **降級友善**:無 redis→`no-redis` 不崩潰;無 aiogram→healthz/metrics 可用;無 token→feed 回 False
3. **sanitize 預設拒絕**:高風險(注入/命令/路徑)直接拒絕,PII 遮罩
4. **webhook 不碰 AI**:長任務全走佇列,多用戶並發不互卡

## 後續建議
- 部署時以 `python telegram_bot.py`(uvicorn)+ `python worker.py --workers N` 兩程序
- 公開前設定真實 webhook URL(`bot.set_webhook`),並以 `/api/webhook` 為 Telegram update 入口