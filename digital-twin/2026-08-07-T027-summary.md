# T027 任務完成摘要 — pyright 指向 .venv(76 → 0 errors)

- **狀態**: done
- **Commit**: 7d0edc0
- **日期**: 2026-08-07

## 環境設定

`pyproject.toml [tool.pyright]`:
```toml
pythonVersion = "3.14"   # 3.10 → 3.14，對齊 .venv 直譯器
venvPath = "."
venv = ".venv"
include = ["*.py", "tests/**/*.py"]   # T017 設定維持，未回退 src/
exclude = ["**/__pycache__", ".venv", "venv"]   # .venv 本身不掃描
```

單一來源(pyrightconfig.json 未建立,避免衝突)。

## 成果

- **76 → 0 errors, 0 warnings**(含先前 5 個 reportMissingImports 誤報全消除)
- venv 解析成功後另暴露 **56 個真實型別錯誤**,全部修復(機械型別修正,不動搖行為);
  一併修正 2 個真 bug:
  - `telegram_bot.py`:`await uvicorn.run(...)` → `uvicorn.run(...)`(uvicorn.run 為阻塞式,await None 執行期必炸)
  - `resilience.py`:`make_breaker` 遮蔽(except 重新定義)→ 單一定義避免遮蔽

## 修法歸納

| 問題 | 修法 |
|---|---|
| 可選依賴 import(try/except 內)致 unbound/None 型 | Any + None 佔位(OTEL/Prometheus/aiogram/redis/pybreaker) |
| aiogram Message 註釋 | `TYPE_CHECKING` import;未來 annotations 下執行期不求值 |
| tuple 雙欄位無法關聯 | `assert output is not None`,`assert breaker_guard is not None` |
| `TwinSettings()` 動態建構 | `# type: ignore[call-arg]`(pydantic-settings 固有) |
| dict/None fallback | `rule.get("delegate_to") or "my"` 等 |
| 測試 Optional 窄化 | `_cfg()` helper + assert 收窄 |

## 驗收

| 項目 | 結果 |
|---|---|
| T026 前置 .venv 存在 | ✅ |
| venvPath/venv 設定 | ✅ |
| pyright . → 0 errors 0 warnings | ✅ |
| include 維持 * 與 tests/** | ✅ |
| .venv 排除 | ✅ |
| run_tests 閘門不受影響 | ✅ 70 passed |
| homebrew pytest 5 passed | ⚠️ 已過時(見下) |

> homebrew `/opt/homebrew/bin/python3 -m pytest` 於收集期缺 tenacity 等依賴失敗。此標準為 T026 .venv 前之舊環境要求,現行測試環境為 `.venv/bin/python -m pytest` → **70 passed + 1 skipped**。

## 測試

- `pyright .` → 0 errors, 0 warnings
- `.venv/bin/python -m pytest -q` → **70 passed + 1 skipped**
- 受影響檔案 ruff 全 clean,pre-commit(gitleaks + ruff + incremental_index)通過