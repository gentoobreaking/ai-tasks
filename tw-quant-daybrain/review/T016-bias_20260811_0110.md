# T016 完成記錄（2026-08-11 01:0x）

## 目標
實作 §5 盤前多空傾向鎖定（Bias Decision Tree），08:55 鎖定 LONG_ONLY/SHORT_ONLY/NEUTRAL_FLEXIBLE/NO_TRADE 四態。

## 關鍵成果
1. `src/bias/decision_tree.ts`（+`decision_tree.test.ts` 17 tests）— commit `1186855`
2. 全套 239/239 tests pass、lint/type check 過
3. 任務書 frontmatter status: done + 6/6 驗收勾選、README T016 checkbox、`~/tasks/tw-quant-daybrain/T016-summary.md` 寫入

## 重要決策：tw-quant-mcp 契約落差處理
對接探查（真實 binary `~/Projects/tw-quant-mcp/bin/tw-quant-mcp-v1.3.0`，37 tools）發現 spec §2.2 的 18 tools 子集與實際不符：
- 實際**無** `get_pre_market_quote` / `get_taifex_night` / `get_us_market`
- 法人籌碼實際簽名：`get_institutional_investors({ market })` → `data.rows[]`（code/total_net）
- 夜盤實際來源：`get_futures_daily_ohlc({ contract:'TX' })` → session=盤後 change_pct
- 日 K：`get_stock_daily_kline` 的 data 是日K陣列（非包一層），可自行算 MA5/MA20
- 非交易時段盤中工具回 isError（非 JSON）→ 守門/呼叫例外處理

處理：法人用當日 total_net 代理近 3 日（rationale 註記）；夜盤用 futures 盤後；美股/試撮節點 0 分 + 註記（不假設不存在的工具）。

## 待續
T017（VWAP_SURGE_LONG 做多策略引擎，§6）→ T018（BULL_TRAP_VWAP_SHORT 空方策略，§7）。T017/T018 需注意：spec §6/§7 的進場四條件涉及 get_intraday_vwap/detect_volume_surge/get_intraday_kline/get_futures_daily_ohlc（實際契約都有），但「台指 1 分 K 紅棒」需確認實際工具（get_intraday_kline 是否支援 TX？）。
