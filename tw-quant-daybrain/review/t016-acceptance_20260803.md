# T016 程式碼品質改善 — 驗收完成紀錄

日期: 2026-08-03 | Commit: `da78c7e` (branch main)
專案: ~/Projects/tw-quant-signal | 任務書: ~/tasks/tw-quant-signal/tasks/T016-code-quality-fixes.md

## 目標
修正 Review 發現的 feature stale / latency / redundant API 問題，管線 ~45s → ~20s。

## 完成內容（5 檔，+367/-164）

### §1 Feature 過期修復 (rules.py, backtest.py)
- `_load_features(db, trade_date=None)`：GROUP BY stock_id + MAX(trade_date) 子查詢 JOIN，支援 trade_date <= 過濾
- `compute_rule_signals()` 正確傳入 trade_date
- backtest `_compute_features_as_of` 改用 `_load_features(db, trade_date=td)`，避免取到未來特徵

### §2 Valuation 重複呼叫消除 (twse_client.py, features.py, ingestion.py)
- 新增 `fetch_valuations_all()`（BWIBBU_ALL 一次拉 1081 檔）
- 新增 `_ingest_valuations()`（暫存 `_latest_valuations`），run_daily 加入 valuations 階段
- `_stock_features(db, sid, val, indicators)` 接收外部 val，不再 per-stock 呼叫

### §3 月營收批次平行化 (twse_client.py)
- `fetch_monthly_revenue_batch()` async 化：httpx.AsyncClient + Semaphore(_MOPS_CONCURRENCY=3)
- 實測：36 個月 28.0s → 7.3s（74% 降幅 > 60% 目標）
- 新增增量模式 `incremental=True`：只抓 DB 缺少且已公告月份（MOPS 約每月 10 日公告），日常運行近乎零 HTTP

### §4 每日計算 incremental (features.py, ingestion.py)
- 新增 `compute_indicators_for_stock(db, sid, lookback=120, full=False)`，每日只算 120 天（省 67%）；backfill 用 full=True
- `_ingest_indicators()` 改用並暫存 indicators_map 供 features 階段

### §5 錯誤處理與重試 (twse_client.py, ingestion.py)
- `_retry/_retry_async`（max 3 retry、指數 backoff base 0.8s max 5s）+ `_request_json/_request_json_async` 統一入口
- fetch_daily_prices_all / fetch_market_index / fetch_institutional_flows / fetch_valuations / fetch_historical_daily_prices 全部接入
- fail/skip 一致化：monthly_revenue 空結果（未公告）不再誤報 fail

## 量測
- 管線 10 階段全 ok，總耗時 ~17s（原 45s+，達 ~20s 目標）
- 反爬阻塞時約 2m（外部限制）

## 已知坑（重要保留）
- MOPS 時間性 IP 封鎖：30s 內過多請求觸發 FOR SECURITY REASONS，約 90s 清除
- 採對策：單股票內部平行（不跨股票）、並行度 3、增量模式

## 驗收測試
- §1 `_load_features(trade_date)` as_of 過濾正確；compute_rule_signals 3 筆訊號
- 回測 105 天/30 規則/3d forward 無錯誤
- py_compile 全過、git commit 完成、任務書 status: pending → done
